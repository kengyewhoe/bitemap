# Copy to .env (git-ignored) and fill in from the Supabase project settings.
# Read in js/api.js via import.meta.env once the real client is switched on.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
